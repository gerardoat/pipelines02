pool:
  vmImage: 'ubuntu-latest'

variables:
  build: $(Build.BuildNumber)

steps:
- script: |
    sudo export IDBUILD=$(build)
    sudo export ID="${IDBUILD/./-}"
    echo "Aqui si funciona $ID"
- script: echo "Aqui ya no funciona $ID"
