pool:
  vmImage: 'ubuntu-latest'

variables:
  build: $(Build.BuildNumber)

steps:
- script: |
    export IDBUILD=$(build)
    export ID="${IDBUILD/./-}"
    $(mivar) = $ID
    echo "Aqui si funciona $(mivar)"
- script: echo "Aqui ya no funciona $(mivar)"
